# Log

## 2026-09-02

* **Decision** — MVP is a single market (one board) — Tom, 2026-09-02. Multi-board stays in the data model (`boardId` everywhere), out of MVP scope.
* **Update** — Display layout corrected per Tom's sample board: sponsor tile moved to the CENTER column (promoter tile + QR tile, flanked by frame columns left/right; was: promoter bottom-right). Tiles must match the sample exactly — the sample screenshot could not be rendered this session (no image support); structure extracted via OCR + pixel analysis (`scripts/analyze-*.mjs` kept for reference): header strip, dense tile grid, center sponsor tile (REWE at image center), dense bottom strip. Tom to confirm exact tile geometry — current layout is interim.
* **Update** — Spec 012 completed: legal pages + board seed live — `/impressum` + `/datenschutz` (German, linked from all four public pages; operator identity marked PLATZHALTER for Tom to fill in before going public), `scripts/seed-board.mjs` (idempotent upsert, PBKDF2 hash same format as the Worker, verified cross-implementation by a test; run via `node scripts/seed-board.mjs --remote --password <pw>`). Production board b1 seeded: **the prod admin login now works** (password communicated to Tom outside the repo — the repo is public). Promoter fields live in the feed (REWE FAMILIE SCHULZE / slogan — mockup wording, verify-me). TDD: 5 new tests — all 120 green. Verified locally + prod (seed ran twice → one row, login 200, legal pages 200).
* **Skip** — Spec 011 (expiry cron) skipped: blocked by 004 (R2). Post expiry itself is R2-free, but the photo grace cleanup is core to the spec; re-do when R2 is enabled.
* **Update** — Spec 010 completed: poster page `/p/{postId}?t={token}` live — GET returns status + approved comments (token hashed & compared against `mgmt_token_hash`; wrong/missing token = same 404 as unknown id, nothing leaked), DELETE marks the post `deleted` (status change → feed drops it on the next ≤30 s poll), page shows German status ("wird geprüft" / "ist live" / "abgelehnt" / "gelöscht"), approved comments, delete button with confirm (hidden for rejected/deleted). TDD: 16 new tests (routes incl. no-leak check, jsdom page incl. confirm decline, route) — all 114 green. Verified locally (create → approve → live → delete → feed excludes) and in production (same flow, temp row cleaned up).
* **Update** — Spec 009 completed: approved comments render under their frame on the display — newest 3 in chronological order (feed delivers them oldest-first, frame takes `slice(-3)`), escaped, subtle list styling, arrives via the normal ≤30 s poll. TDD: 3 new frame tests + display-page fixtures — all 98 green. Verified locally and in production end-to-end: comment → admin approve → feed serves it → visible on https://machi-15.machi-15.workers.dev/b/b1 (left one approved comment on the demo post as a fixture). Rejected comments never appear (feed only serves `live` comments — enforced in 006).
* **Update** — Spec 008 completed: comments live — `POST /api/posts/{id}/comments` (body ≤500 chars, post must be live AND non-expired → German 404 otherwise, per-IP rate limit 5/10 min via new `comments.ip_hash` column, migration 0004; lands `pending` in the admin queue), comment form page `/b/{boardId}/p/{postId}` (post looked up in the public feed, German confirmation "Vielen Dank! Dein Kommentar wird geprüft."), display frames now carry a small QR to their comment form (bundled lib, 007 extended). Shared `postWithRetry` moved to `public/ui.js` (submit + comment pages). TDD: 13 new tests + display updates — all 95 green. Verified locally (201, 404×3, queue visibility, 429 on 6th) and in production (201 → pending in admin queue → rejected via admin API as cleanup, page 200).
* **Update** — Spec 007 completed: TV display `/b/{boardId}` live — header "Machi-Board (Display)" (mockup OCR wording, still on the verify-me list for Tom), frames grid (title, body, photo when `photoKey` set, contacts, comment-count badge), big QR tile "Starte hier Dein kostenloses Inserat!" → `/b/{boardId}/neu`, promoter tile from board config, 25 s feed polling, localStorage cache of the last feed + subtle offline hint with auto-retry (poll keeps trying). 55" CSS: large type, high contrast, no autoplay, no websockets. TDD: 16 new tests (frame rendering + escaping, jsdom incl. fake-timer poll and offline paths, routes). Shared `public/qr.js` + `public/ui.js` extracted — submit/admin reuse them. Verified locally and in production (200, submit path intact, feed serving the live fixture). Note: photos (frames + promoter logo) render once 004 lands — img tags are already wired to `/api/photos/{key}`.
* **Update** — Spec 006 completed: `GET /api/boards/{id}/feed` live — board config (name + promoter fields) + live posts newest-first, each with approved comments; expiry filter (live-but-expired excluded), pending/rejected never appear; 478-byte response locally (poll-friendly). TDD: 5 new route tests against the stateful fake D1 (shared `mapPost` extracted to `src/post-mapper.ts`). Verified locally (wrangler dev) and in production (deploy + curl: board config + live fixture post, 404 unknown board).
* **Update** — Spec 005 completed: admin API + page live at `/admin/{boardId}` — PBKDF2-SHA256 password hashes (100k iterations, format `pbkdf2-sha256$iters$salt$hash` — the 012 seed will use it), D1-backed sessions (migration 0003, 7-day TTL, board-scoped, token hashed at rest), generic German 401 on wrong password, pending posts + comments, live list, approve/reject (approve sets `expires_at`), delete (comments first). TDD: 30 new tests (hash format, stateful fake-D1 route tests, jsdom page incl. logout/401 handling). Verified locally end-to-end with a real password and in production (page 200, generic 401, session minted via `d1 execute` → pending/live/approve/reject/delete against remote D1). **Important for Tom:** production board `b1` still carries the placeholder hash `seed`, so the prod admin page cannot be logged into until spec 012 runs its seed. Prod data cleaned up during verification (junk rejected; one live test post kept as 006/007 fixture). Deferred: per-IP login rate limiting.
* **Update** — Spec 003 completed: submit page `/b/{boardId}/neu` live — German mobile-first form (category, title, body, optional contact fields, 1/2-week duration), client-side photo resize to ≤1600 px JPEG via canvas, confirmation screen with management link + QR (qrcode-generator vendored into `public/vendor/`, no QR service). TDD: 19 new tests (pure logic, jsdom wiring against the real HTML, route via assets binding) — all green locally and in production (curl 200 + POST 201). Note: photo upload itself stays in 004 (R2); resized blob is prepared client-side only. Real-phone-browser pass still open (manual).
* **Skip** — Spec 004 (photo storage) skipped: `wrangler r2 bucket create` still fails with code 10042 ("Please enable R2 through the Cloudflare Dashboard"). Spec 011 (expiry cron) is blocked by 004. Both stay in `created/`, marked blocked in the index. Proceeding with 005.
* **Update** — Spec 002 completed: `POST /api/posts` live in production — validation with German 400s, board existence check, per-IP rate limit (3/hour, hashed IP), anonymous management token (hashed at rest). TDD: 12 tests + live integration (201/400/429).
* **Update** — Spec 001 completed: Worker + Hono + `/api/health` live at https://machi-15.machi-15.workers.dev (TDD red-green, D1 remote EEUR with boards/posts/comments, static assets, 404s)
* **Decision** — Single Worker with assets binding replaces the separate Pages project (one deploy target, one domain, no CORS)
* **Note** — R2 activation pending in the Cloudflare dashboard (account-level; may ask for payment info). Unblocks spec 004.
* **Decision** — React reconsidered for the frontend and rejected for the MVP: all four pages stay vanilla HTML/CSS/JS (TV-browser safety on the display, no build step, smallest failure surface). Revisit when the admin grows or platform features arrive.
* **Update** — Specs reorganized into `created/` and `completed/` folders — the folder is the state (replaces the frontmatter status field)
* **Decision** — Spec workflow: small numbered specs in wiki/specs/ (001–012), status `created` → `completed`, built in number order
* **Creation** — wiki/specs/ created: 12 numbered specs + roadmap index (001 bootstrap → 012 legal/seed)
* **Creation** — MVP spec written to wiki/architecture/mvp-spec.md (the overview contract)
* **Update** — Display layout from Tom's mockup screenshot: header "Machi-Board (Display)", promoter tile bottom-right (mockup: REWE / FAMILIE SCHULZE / "Mehr Nähe geht nicht.")
* **Update** — boards table gains promoter fields: promoter_name, promoter_logo_key, promoter_slogan
* **Decision** — Tech stack: vanilla HTML/CSS/JS frontend; Workers + TypeScript + Hono; D1; R2; QR = plain URLs
* **Creation** — OKF knowledge base initialized under `wiki/` (business/, architecture/, references/)
* **Creation** — Repo cloned to E:\Programmieren\machi-15 (repo was empty)
* **Decision** — Deployment: Cloudflare Pages + Workers + D1 + R2, free tier (see architecture/deployment.md)
* **Decision** — Moderation: admin approves posts AND comments before anything is public (see architecture/constraints.md)
* **Decision** — Anonymous posting: no accounts, no email; per-post management token (see architecture/data-model.md)
* **Decision** — Photos resized client-side to ≤1600 px before upload; pending photos private (see architecture/components.md)
* **Decision** — QR codes are plain URLs, one per board; no QR service (see architecture/components.md)
* **Decision** — Design part 1 (architecture & data model) confirmed in brainstorming; part 2 pending
